// src/pages/LoginPage.tsx

// Placeholder — wiring this form to useAuth().login() belongs to a
// later step.
export default function LoginPage() {
  return (
    <div className="mx-auto max-w-sm rounded-2xl border border-line bg-panel/60 p-6">
      <h1 className="font-display text-xl font-semibold text-ink">Log in</h1>
      <p className="mt-2 text-sm text-ink-muted">Login form goes here.</p>
    </div>
  );
}