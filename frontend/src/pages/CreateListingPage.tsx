// src/pages/CreateListingPage.tsx

// Placeholder — the listing form (with image upload to
// /api/listings/:id/images/) belongs to a later step.
export default function CreateListingPage() {
  return (
    <div className="mx-auto max-w-xl rounded-2xl border border-line bg-panel/60 p-6">
      <h1 className="font-display text-xl font-semibold text-ink">Post a listing</h1>
      <p className="mt-2 text-sm text-ink-muted">Listing creation form goes here.</p>
    </div>
  );
}