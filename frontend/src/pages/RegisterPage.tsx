// src/pages/RegisterPage.tsx

// Placeholder — wiring this form to POST /api/auth/register/ belongs to
// a later step.
export default function RegisterPage() {
  return (
    <div className="mx-auto max-w-sm rounded-2xl border border-line bg-panel/60 p-6">
      <h1 className="font-display text-xl font-semibold text-ink">Sign up</h1>
      <p className="mt-2 text-sm text-ink-muted">Registration form goes here.</p>
    </div>
  );
}